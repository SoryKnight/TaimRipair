using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Animation : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // if(veredict.win){
            // Instantiate and play win animation with X 3Dmodel
        // }else if(veredict.lose){
            // Instantiate and play death animation with X 3Dmodel
        // }
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
